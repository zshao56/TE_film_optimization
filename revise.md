1. ~~固定下表面是热端，上表面是空气自然对流冷端。其中热端冷端的温度是作为变量的适用于不同的场景。边缘也考虑边界条件空气自然对流。~~

2. ~~请考虑3D的薄膜结构，有x，y，z三个量，而不仅仅是L和h。那么不同材料的结构变化会更大。~~

3. ~~仅考虑采样温度的二维面积即w，而不用考虑t。~~

4. ~~仅考虑Delta T_{\parallel}作为优化目标，其中热端和冷端的温度都是作为变量（因为我们想要的是在已知条件下获得最大温差，这里的已知条件已经包含了温度还有尺寸等数据

5. ~~把平面结构纳入考虑，左右和对角~~

   已实现：`structured_library.py` 新增 `planar_split` 族（一整块长方体被竖直界面切成两部分，左右 / 前后 / 对角拼接，两种材料各自贯穿全厚度），已注册进 `STRUCTURED_FAMILIES`，六族各约 1/6。
   动机：此前数据库上逆向设计出的类楔形结构温差反而不如平面结构。根因是原五族的高热导区都是底面连通、顶面被低热导层整体覆盖；纯竖直分割其实是 `step` 族的极限退化情形，但 `step` 的采样范围（`0.10–0.45` / `0.55–0.95`）永远取不到那两个端点，所以最优拓扑一直落在参数空间角上、从未被采样。

6. ~~现在仅考虑最大温差作为最终的最优化目标~~

   已实现：`configs/v3_standard.json` 的 `training.target_col` 改回 `delta_T_parallel`（K）。`delta_T_parallel_per_area` 仍写入 metadata 但仅作辅助分析列，不参与训练。逆设计排名本来就用真实 FDM 的 `fdm_delta_T`，无需改动。
